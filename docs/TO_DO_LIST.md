# TODO List — 模拟盘实施路线与并行改进

> 最后更新：2026-07-09
> 目标：**尽快上模拟盘**，改造任务与模拟盘并行推进。
> 模拟盘无需 MT5（simulate 模式已可用），核心缺口是 **daily_inference → 执行层的自动对接**。

---

## 一、🚀 最小模拟盘链路（1 周内完成）

这是上模拟盘的**绝对最小路径**。以下任务按执行顺序排列，做完即可跑模拟盘。

```
状态图例:  [ ] 待办  →  [~] 进行中  →  [x] 已完成
```

### Sprint 1：打通自动交易循环

```
[ ] 1.0  修复 blocking bugs
          ├─ [ ] signals.py:98  — min_hold 死代码修复（回测中 min_hold 约束不可靠）
          ├─ [ ] order_manager.py:90 — Broker 私有方法调用改为公共接口
          └─ [ ] broker.py:216  — get_symbol_price bid/ask 方向修复（实盘影响，模拟盘可后做）

[ ] 1.1  编写模拟盘启动脚本 scripts/run_simulation.py
          ├─ daily_inference 生成信号
          ├─ Broker(simulate=True) 执行开/平仓
          ├─ RiskManager 风控检查（杠杆/止损/每日次数）
          ├─ OrderManager 记录审计日志
          └─ 每日状态摘要输出到终端

[ ] 1.2  确认 daily_inference 输出 → 执行层输入的数据契约对齐
          ├─ entry_signal 格式转换为 OrderManager.place_market 调用
          ├─ exit_signal → close_position / close_all
          └─ 增量运行（不清除历史持仓，仅处理新信号）

[ ] 1.3  设置 Windows 定时任务（每天收盘后运行）
          ├─ python scripts/run_simulation.py
          └─ 输出日志到 data/simulation_log/

验收标准:
  ✅ 脚本连续运行 5 个交易日不报错
  ✅ 审计日志有完整开/平/持仓记录
  ✅ 模拟账户权益曲线可追踪
```

### Sprint 2：模拟盘监控（第 2 周）

```
[ ] 2.0  每日摘要输出（日志 + 简单表格）
          ├─ 当前持仓（ticker, 方向, 盈亏, 持有天数）
          ├─ 账户权益曲线
          └─ 风控触发记录（如有）

[ ] 2.1  模拟盘 vs 回测偏差分析
          ├─ 费率偏差（实际 slippage 是否在 0.05% 以内）
          ├─ 成交率（是否所有信号都成功执行）
          └─ 收益偏差原因分类

[ ] 2.2  每周自动生成模拟报告
          ├─ 持仓变化图
          ├─ 胜率/盈亏比
          └─ 与 SPY 基准对比
```

---

## 二、🔄 并行改造任务（与模拟盘同步推进）

以下任务**不阻塞模拟盘上线**，可在模拟盘运行期间按优先级逐步完成。

### 短期（1-2 周）

| 优先级 | 任务 | 说明 | 预估工时 |
|:---:|:---|---:|:---:|
| P0 | VIF 多重共线性测试 | 波动率大类因子高相关风险 | 2h |
| P1 | 安全杠杆 7x 重新评估实验 | 用 scale=7 跑 V1/V2 收益预期 | 1h |
| P2 | 统一 N_BASE_FEATURES 推导 | 从 FEATURE_COLS/FACTOR_NAMES 自动计算 | 0.5h |
| P3 | 建立 pytest 测试套件 | 13 个因子纯函数 + signals 生成 | 4h |
| P4 | 删除 risk.py 未调用的 check_daily_limits | 死代码清理 | 0.5h |
| P5 | 修复 backtest/engine.py active_returns 沉默过滤 | 加日志说明过滤量 | 0.5h |

### 中期（1-2 月）

| 优先级 | 任务 | 说明 |
|:---:|:---|---:|
| P0 | 新一轮因子寻找 | 横截面动量、Beta 相对强弱 |
| P1 | 通用 CLI （argparse 替代 sys.argv） | 覆盖 train_model.py 和 run_backtest.py |
| P2 | backtest/config.py 添加 docstring | 与 models/config.py 对齐 |
| P3 | 因子共线性修复 | VIF > 5 的因子组合并或剔除 |

### 长期（Q4）

| 优先级 | 任务 |
|:---:|---|
| P0 | IC 衰减持续监控 |
| P1 | **极小资金（$500~$1000）实盘起步** |
| P2 | 全量因子 pool 的自动化监控 Dashboard |

---

## 三、📌 关键决策点

| 时机 | 决策 | 选项 |
|:---:|:---|---:|
| 模拟盘运行 1 个月后 | 7x 杠杆下年化 ~10.5% 是否值得上实盘？ | ① 接受并上实盘 / ② 继续改进模型再上 |
| 偏差分析完成后 | 模拟盘与回测偏差是否在可接受范围？ | ① 偏差 < 20% → 准备实盘 / ② 偏差 > 20% → 诊断修复 |
| VIF 测试完成后 | 是否需要剔除高相关因子？ | ① 剔除（降维至 8-10 个因子）/ ② 保留（ML 自动处理共线性） |

---

## 四、📊 状态追踪

| 周次 | 模拟盘链路 | 并行改造 | 备注 |
|:---:|:---|---:|:---|
| W1 (07/09-) | [ ] Sprint 1 | [ ] VIF 测试 + 7x 评估 |  |
| W2 (07/16-) | [ ] Sprint 2 | [ ] pytest + N_BASE_FEATURES |  |
| W3+ | [ ] 持续运行 + 偏差分析 | [ ] 因子寻找 + CLI 统一 |  |

---

## 五、关联文档

- [审查报告.md](审查报告.md) — 当前代码审查详细结果
- [当前记录与更新计划.md](当前记录与更新计划.md) — 实验结论与回测数据
- [工程化改造.md](工程化改造.md) — 架构改造记录与路线图
- [使用指南.md](使用指南.md) — 命令速查与操作说明